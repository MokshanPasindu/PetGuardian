# PetGuardian AI Service

CNN-based pet skin disease detection Flask API.

## Setup

```bash
cd petguardian-ai
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt